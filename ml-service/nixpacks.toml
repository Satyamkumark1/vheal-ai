[phases.build]
cmds = ["pip install -r requirements.txt", "python data/synthetic_train.py"]

[phases.start]
cmds = ["uvicorn main:app --host 0.0.0.0 --port $PORT"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
